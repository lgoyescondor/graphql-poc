# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'graphql-poc' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for graphql-poc
  pod 'Apollo'

  target 'graphql-pocTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'graphql-pocUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
